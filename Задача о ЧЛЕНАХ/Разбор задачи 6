Разбор задачи 6

Условие:

Реализовать операцию умножения много ЧЛЕНОВ  <===8

На вход программе передаётся степени многочленов n и m

Затем вводятся коэффициенты при каждом слагаемом этих много ЧЛЕНОВ.

Считывать коэффициенты через scanf.

Вывести на экран результат их умножения.

Идея решения:

Вспоминаем как мы умножаем много ЧЛЕНЫ на бумажке: просто каждый элемент первого умножаем на каждый элемент второго.
Соответственно мы вводим параметры и создаём массив ответов размером m + n + 1, потому что при умножении степени максимальных 
элементов сложатся.
Затем делаем два вложенных цикла (первый отвечает за первый много ЧЛЕН, а второй за второй)
Дальше просто красиво выводим ответ и всё.
    
    ()    ()    ()
    ()    ()    ()
    ()    ()    ()
    ()    ()    ()
   ()()  ()()  ()()
